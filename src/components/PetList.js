import Pet from "./Pet";

function PetList(){
    return (
        <ul className="pet-list">
            {/* render a list of <Pet> components in here */}
        </ul>
    );
}

export default PetList;